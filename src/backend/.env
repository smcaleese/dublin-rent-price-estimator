# PostgreSQL settings for Docker Compose and FastAPI application
# Replace with your desired secure credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=db
POSTGRES_PORT=5432

# Database URL for the FastAPI application to connect to PostgreSQL
# This should match the user, password, port, and db name above if running backend on host
DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/db"

# JWT Secret Key for FastAPI authentication
# Replace with a long, random, and secret string
SECRET_KEY="secret"
